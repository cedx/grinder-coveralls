name: grinder_coveralls
version: 2.0.0

description: Grinder plug-in collecting your code coverage as LCOV and uploading it to the Coveralls service.
homepage: https://dev.belin.io/grinder-coveralls
issue_tracker: https://github.com/cedx/grinder-coveralls/issues
repository: https://github.com/cedx/grinder-coveralls

environment:
  sdk: '>=2.7.0 <3.0.0'

dependencies:
  code_builder: ^3.2.1
  coverage: ^0.13.3
  coveralls: ^5.6.0
  crypto: ^2.1.4
  glob: ^1.1.7
  grinder: ^0.8.3

dev_dependencies:
  path: ^1.6.4
  test: ^1.11.0
